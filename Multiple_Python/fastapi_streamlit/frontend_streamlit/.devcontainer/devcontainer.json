{
	"name": "Existing Docker Compose (Extend)",

	"dockerComposeFile": [
		"../../docker-compose.yml",
		"docker-compose-dev-in-container.yml"
	],

	"service": "ui",

	"workspaceFolder": "/workspace",

	"settings": {},

	"extensions": ["ms-python.python"], 

	"postCreateCommand": "streamlit run /workspace/frontend_streamlit/main.py"
}
